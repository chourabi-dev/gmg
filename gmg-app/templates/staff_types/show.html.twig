{% extends 'base.html.twig' %}

{% block title %}StaffTypes{% endblock %}

{% block body %}
    <h1>StaffTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ staff_type.id }}</td>
            </tr>
            <tr>
                <th>StaffType</th>
                <td>{{ staff_type.staffType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('staff_types_index') }}">back to list</a>

    <a href="{{ path('staff_types_edit', {'id': staff_type.id}) }}">edit</a>

    {{ include('staff_types/_delete_form.html.twig') }}
{% endblock %}
