<form method="post" action="{{ path('sub_skills_delete', {'id': sub_skill.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_skill.id) }}">
    <button class="btn">Delete</button>
</form>
