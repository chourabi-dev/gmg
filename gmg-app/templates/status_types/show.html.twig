{% extends 'base.html.twig' %}

{% block title %}StatusTypes{% endblock %}

{% block body %}
    <h1>StatusTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status_type.id }}</td>
            </tr>
            <tr>
                <th>StatusType</th>
                <td>{{ status_type.statusType }}</td>
            </tr>
            <tr>
                <th>Ordre</th>
                <td>{{ status_type.ordre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('status_types_index') }}">back to list</a>

    <a href="{{ path('status_types_edit', {'id': status_type.id}) }}">edit</a>

    {{ include('status_types/_delete_form.html.twig') }}
{% endblock %}
