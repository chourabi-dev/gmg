<form method="post" action="{{ path('family_status_types_delete', {'id': family_status_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ family_status_type.id) }}">
    <button class="btn">Delete</button>
</form>
