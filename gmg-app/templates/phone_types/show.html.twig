{% extends 'base.html.twig' %}

{% block title %}PhoneTypes{% endblock %}

{% block body %}
    <h1>PhoneTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phone_type.id }}</td>
            </tr>
            <tr>
                <th>PhoneType</th>
                <td>{{ phone_type.phoneType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('phone_types_index') }}">back to list</a>

    <a href="{{ path('phone_types_edit', {'id': phone_type.id}) }}">edit</a>

    {{ include('phone_types/_delete_form.html.twig') }}
{% endblock %}
