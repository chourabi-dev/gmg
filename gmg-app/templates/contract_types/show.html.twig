{% extends 'base.html.twig' %}

{% block title %}ContractTypes{% endblock %}

{% block body %}
    <h1>ContractTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contract_type.id }}</td>
            </tr>
            <tr>
                <th>ContractType</th>
                <td>{{ contract_type.contractType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contract_types_index') }}">back to list</a>

    <a href="{{ path('contract_types_edit', {'id': contract_type.id}) }}">edit</a>

    {{ include('contract_types/_delete_form.html.twig') }}
{% endblock %}
