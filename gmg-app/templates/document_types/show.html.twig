{% extends 'base.html.twig' %}

{% block title %}DocumentTypes{% endblock %}

{% block body %}
    <h1>DocumentTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document_type.id }}</td>
            </tr>
            <tr>
                <th>DocumentType</th>
                <td>{{ document_type.DocumentType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('document_types_index') }}">back to list</a>

    <a href="{{ path('document_types_edit', {'id': document_type.id}) }}">edit</a>

    {{ include('document_types/_delete_form.html.twig') }}
{% endblock %}
