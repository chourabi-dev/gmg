{% extends 'base.html.twig' %}

{% block title %}RelativeTypes{% endblock %}

{% block body %}
    <h1>RelativeTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ relative_type.id }}</td>
            </tr>
            <tr>
                <th>RelativeType</th>
                <td>{{ relative_type.RelativeType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('relative_types_index') }}">back to list</a>

    <a href="{{ path('relative_types_edit', {'id': relative_type.id}) }}">edit</a>

    {{ include('relative_types/_delete_form.html.twig') }}
{% endblock %}
