{% extends 'base.html.twig' %}

{% block title %}DocumentRef{% endblock %}

{% block body %}
    <h1>DocumentRef</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document_ref.id }}</td>
            </tr>
            <tr>
                <th>DocumentRef</th>
                <td>{{ document_ref.DocumentRef }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('document_ref_index') }}">back to list</a>

    <a href="{{ path('document_ref_edit', {'id': document_ref.id}) }}">edit</a>

    {{ include('document_ref/_delete_form.html.twig') }}
{% endblock %}
