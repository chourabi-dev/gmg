{% extends 'base.html.twig' %}

{% block title %}MissionTypes{% endblock %}

{% block body %}
    <h1>MissionTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mission_type.id }}</td>
            </tr>
            <tr>
                <th>MessionType</th>
                <td>{{ mission_type.messionType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mission_types_index') }}">back to list</a>

    <a href="{{ path('mission_types_edit', {'id': mission_type.id}) }}">edit</a>

    {{ include('mission_types/_delete_form.html.twig') }}
{% endblock %}
