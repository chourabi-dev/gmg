{% extends 'base.html.twig' %}

{% block title %}SourceTypes{% endblock %}

{% block body %}
    <h1>SourceTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ source_type.id }}</td>
            </tr>
            <tr>
                <th>SourceType</th>
                <td>{{ source_type.sourceType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('source_types_index') }}">back to list</a>

    <a href="{{ path('source_types_edit', {'id': source_type.id}) }}">edit</a>

    {{ include('source_types/_delete_form.html.twig') }}
{% endblock %}
